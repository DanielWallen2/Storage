@model IEnumerable<Storage.Models.ProductViewModel>

@{
    ViewData["Title"] = "Inventory";
}

<h1>Inventory</h1>

<p>
    <a asp-action="Create">Lägg till Ny</a>

    <form asp-action="Filter2">
        <div class="input-group mb-3">
        <input type="text" name="name" class="form-control" placeholder="Sök på produktnamn"/>
        <div class="input-group-append">
            <select class="form-control" name="category" asp-items="@Html.GetEnumSelectList<Category>()">
                <option selected="selected" value="">Välj kategori</option>
            </select>
        </div>
        <div class="input-group-append">
            <button type="submit" class="btn btn-outline-secondary"><i class="fa fa-search">&emsp;Sök</i></button>
        </div>
        </div>
    </form>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Count)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InventoryValue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Count)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InventoryValue)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |*@
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
        <tr>
            <td>
                @Html.Raw("Totalt lager värde")
            </td>
            <td></td>
            <td></td>
            <td>
                @Model.Select(v => v.InventoryValue).Sum()
            </td>
            <td></td>
        </tr>

    </tbody>
</table>

@*<h1>@ViewData["fsf"]</h1>*@
@*@ViewBag.*@



